<h1>Worker list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Lastname</th>
            <th>Name</th>
            <th>Middlename</th>
            <th>Specialty</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('worker_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.lastname }}</td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.middlename }}</td>
            <td>{{ entity.specialty }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('worker_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('worker_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('worker_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
